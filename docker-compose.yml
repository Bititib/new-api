services:
  new-api:
    container_name: new-api
    image: calciumion/new-api:latest
    ports:
      - "3000:3000"
    environment:
      TZ: Asia/Shanghai
    volumes:
      - ./data:/data
    restart: unless-stopped
